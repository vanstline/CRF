import React, { Component } from 'react';
import styles from './index.scss';

export default class BottomLogo extends Component {
  render() {
    return (
      <div className={styles.shareMain}>
        <span className={styles.logo}>logo</span>
      </div>
    )
  }
}
